---
name: cleanup
description: Audit generated artifacts without changing the working tree
---

<!-- Generated by GG Coder /setup generated-audit v1 -->

This command is read-only: it reports generated artifacts and must never delete or alter them.

Require exactly one target from this evidence-backed list:

- `cache`
  - `Matey/.eslintcache` — Matey/package.json lint script enables this cache; Matey/package.json lint:fix script enables this cache (evidence: Matey/package.json)
  - `gg-app/.eslintcache` — gg-app/package.json lint script enables this cache; gg-app/package.json lint:fix script enables this cache (evidence: gg-app/package.json)
- `packages`
  - `Matey/out` — Matey/package.json build script and package entry points declare out (evidence: Matey/package.json)
  - `packages/gg-agent/dist` — packages/gg-agent/package.json build script and package entry points declare dist (evidence: packages/gg-agent/package.json)
  - `packages/gg-ai/dist` — packages/gg-ai/package.json build script and package entry points declare dist (evidence: packages/gg-ai/package.json)
  - `packages/gg-boss/dist` — packages/gg-boss/package.json build script and package entry points declare dist (evidence: packages/gg-boss/package.json)
  - `packages/gg-core/dist` — packages/gg-core/package.json build script and package entry points declare dist (evidence: packages/gg-core/package.json)
  - `packages/gg-editor-premiere-panel/dist` — packages/gg-editor-premiere-panel/package.json build script and package entry points declare dist (evidence: packages/gg-editor-premiere-panel/package.json)
  - `packages/gg-editor/dist` — packages/gg-editor/package.json build script and package entry points declare dist (evidence: packages/gg-editor/package.json)
  - `packages/gg-voice/dist` — packages/gg-voice/package.json build script and package entry points declare dist (evidence: packages/gg-voice/package.json)
  - `packages/ggcoder-eyes/dist` — packages/ggcoder-eyes/package.json build script and package entry points declare dist (evidence: packages/ggcoder-eyes/package.json)
  - `packages/ggcoder/dist` — packages/ggcoder/package.json build script and package entry points declare dist (evidence: packages/ggcoder/package.json)
- `sidecar-deps`
  - `gg-app/src-tauri/sidecar` — gg-app/package.json bundle:sidecar script generates the staged sidecar; gg-app/src-tauri/.gitignore marks the staged sidecar output; gg-app/src-tauri/tauri.conf.json bundles sidecar/ as a resource (evidence: gg-app/package.json, gg-app/src-tauri/.gitignore, gg-app/src-tauri/tauri.conf.json)
- `tauri`
  - `gg-app/src-tauri/target` — gg-app/src-tauri/.gitignore marks Cargo target output; gg-app/src-tauri/Cargo.toml declares the Tauri Cargo package (evidence: gg-app/src-tauri/.gitignore, gg-app/src-tauri/Cargo.toml, gg-app/src-tauri/tauri.conf.json)
- `tauri-schemas`
  - `gg-app/src-tauri/gen/schemas` — gg-app/src-tauri/.gitignore marks generated schemas; gg-app/src-tauri/tauri.conf.json declares Tauri configuration (evidence: gg-app/src-tauri/.gitignore, gg-app/src-tauri/tauri.conf.json)
- `web`
  - `gg-app/dist` — gg-app/package.json build script runs vite build; gg-app/src-tauri/tauri.conf.json build.frontendDist points to ../dist (evidence: gg-app/package.json, gg-app/src-tauri/tauri.conf.json)

Run `node scripts/audit-generated.mjs <target>` for the selected target and report its complete output.
Treat `Refusing to audit ...` and every Git/spawn error as a failed audit.
Run `git status --short` before and after only to confirm the audit made no changes.
Do not follow the audit with deletion, cleanup, or any other artifact mutation.
